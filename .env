# Paste your jev API key after the = (get one at https://console.typesafe.ai/settings/keys)
# then run `npm run sync-key` so the extension can pick it up.
# This file is skipped from git once you run sync-key, so your key stays on your machine.
TYPESAFE_API_KEY=
