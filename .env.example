# Copy this file to .env and paste your jev API key after the =.
# Keys live at https://console.typesafe.ai/settings/keys
# Then run `npm run sync-key` to write src/config.local.js (git-ignored).
TYPESAFE_API_KEY=
